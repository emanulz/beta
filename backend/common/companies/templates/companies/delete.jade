|{% extends "layout/base_layout.html" %}

| {% load i18n static %}

| {% block content %}

form(action='' method='post')

    |{% csrf_token %}

    p Desea eliminar el Objeto {{ object.code}} - {{object.description}} ?

    input(type='submit' value='Eliminar')

| {% endblock %}

